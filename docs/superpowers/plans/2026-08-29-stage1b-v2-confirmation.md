# Stage1B v2 双尺度世界确认实施计划

1. 保存上一轮完整运行及失败原因，明确其为探索性筛选证据。
2. 创建只包含双尺度世界、iTransformer、VAR、新确认种子和 h1–6 主要判断的 v2 配置。
3. 让配置加载、任务编译、运行入口和门禁共同读取同一个可审计配置路径。
4. 将服务器准入从固定双 4090 改为实际资源探针；保留单卡 20 GiB 以上、CPU、内存、磁盘和 24 小时可行性门禁。
5. 为确认运行使用独立 artifact root，确保检查点、数据库、证据和旧运行互不覆盖。
6. 更新权威变更单、实施书交叉引用、Stage1B 资格说明和服务器 runbook。
7. 运行配置、编译、门禁、环境、调度、监控和续跑测试；不在本机执行完整训练。
8. 服务器开启后先 preflight，再由独立进程 launch；用户通过只读前端监督，断电后手动 resume。
