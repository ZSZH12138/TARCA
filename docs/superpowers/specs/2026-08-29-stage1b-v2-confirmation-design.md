# Stage1B v2 双尺度世界盲确认设计

日期：2026-08-29
状态：已授权、待服务器盲确认

## 目的

把上一轮完整运行作为探索性筛选证据保留，再用一套事前固定、互不重叠的新种子确认：`lorenz96_twoscale_v2` 的短期预测是否适合作为后续机制解释对象。

## 科学范围

- 世界：仅 `lorenz96_twoscale_v2`。
- 神经候选：仅官方 iTransformer 适配器；基线仍为调优 VAR。
- 主要判断：h1–6。
- 次要报告：h7–12，只描述泛化趋势，不改变主要结论。
- 失败边界：h13–24，只描述能力边界，不要求神经模型获胜。
- 新确认根种子：`1649910005`、`2058661680`、`723243092`。
- 种子生成规则：`SHA256("TARCA|Stage1B|v2-r2|confirmation|i")` 的前 8 字节取大端整数，再对 `2**31-1` 取模，`i=1..3`。
- E01/E02 正式种子保持封存，不运行 E01/E02。

## 判断规则

CRPS 仍是主要概率预测指标。h1–6 必须满足既有的比较单元数量、胜率、技能分数、三种子覆盖、已见/未见状态多数胜出、最坏状态、NLL、MAE、概率尺度和机制可操作性护栏。校准不再要求相对 VAR 更好，而要求神经模型自身的名义 90% 区间平均绝对覆盖误差不超过 0.05。

## 防止看结果后改口

上一轮结果明确标记为 pilot selection，不能计入本轮确认。新配置、种子、主要时距和护栏必须在启动训练前写入配置并哈希。运行中前端只显示进程、资源、进度、检查点和 ETA，不显示中间 CRPS、胜率或科学结论。

## 运行与恢复

任务由独立服务器进程持续执行，不依赖 Codex 或 SSH 会话存活。调度器每次按实际 CPU、内存、GPU、显存和磁盘生成容量，不绑定具体型号或卡数。神经训练周期性写原子检查点；断电后由用户或 Codex明确执行 `resume`，不自动续跑。

## 冻结结果

若确认通过，冻结的是 v2-r2 的“世界 + iTransformer + VAR + h1–6 + 新种子 + 配置/代码哈希”整体。正常情况下不可修改，但用户可以通过新的授权变更单修改或覆盖。若失败，只保存失败证据，不将其冻结为新的有效世界。
