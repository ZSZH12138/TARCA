[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "tarca"
version = "0.0.0"
description = "Temporal causal abstraction research contracts and Stage 0 governance tooling"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "pydantic>=2.12,<3",
  "pyyaml>=6.0,<7",
]

[project.optional-dependencies]
research = [
  "einops>=0.7,<1",
  "numpy>=1.26,<3",
  "pot==0.9.7",
  "pyarrow>=25,<26",
  "pyvene==0.1.8",
  "torch==2.13.0",
]
runtime = [
  "fastapi>=0.116,<1",
  "nvidia-ml-py>=13.580,<14",
  "psutil>=7,<8",
  "uvicorn[standard]>=0.35,<1",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
  "mypy>=1.18,<2",
  "pip-audit>=2.9,<3",
  "pre-commit>=4.3,<5",
  "pytest>=9.0,<10",
  "pytest-cov>=7.0,<8",
  "ruff>=0.15,<0.16",
  "types-PyYAML>=6.0,<7",
  "types-psutil>=7,<8",
]

[tool.hatch.build.targets.wheel]
packages = ["src/tarca"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers -ra"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["tarca"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["tarca"]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", marker = "sys_platform == 'win32' or sys_platform == 'linux'" },
]
